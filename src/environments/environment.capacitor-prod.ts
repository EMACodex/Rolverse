/**
 * Configuracion de produccion para Capacitor/APK.
 * La app movil empaqueta Angular y consume el backend remoto por HTTP(S).
 */
export const environment = {
  production: true,
  apiUrl: 'https://rolverseproyecto.onrender.com',
};
