/**
 * Configuracion de desarrollo local.
 * La web servida con `ng serve` consume el backend Node/Express local.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
